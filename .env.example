# WCB API Configuration
VITE_# WCB_API_ENDPOINT configured per environment
# API key now secured in backend
# WCB_API_KEY moved to backend - never expose in frontend


# Other Configuration
NODE_ENV=development # Removed duplicate - use VITE_WCB_API_ENDPOINT only
