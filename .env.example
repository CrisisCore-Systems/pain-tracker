# ================================
# Pain Tracker Environment Configuration
# ================================
# 
# Welcome to Pain Tracker! This file contains all the environment variables
# needed to run the application in development mode.
#
# 🚀 QUICK START:
# 1. Copy this file to .env: `cp .env.example .env`
# 2. Update the values below as needed for your setup
# 3. Run `npm run doctor` to validate your configuration
# 4. Start developing with `npm run dev`
#
# 📚 Need help? Check the README.md or run `npm run doctor`

# ================================
# Frontend Configuration (VITE_* prefix exposes to client)
# ================================

# API endpoint for WorkSafe BC integration
# In development, this typically points to your local proxy or mock server
# In production, this should point to the actual API gateway
VITE_WCB_API_ENDPOINT=/api/wcb

# Application environment - affects build behavior and feature flags
# Options: development, staging, production
# - development: enables debug features, verbose logging
# - staging: production-like but with additional logging
# - production: optimized build, minimal logging
VITE_APP_ENVIRONMENT=development

# Application title (optional) - shown in browser tab and headers
# Leave empty to use default "Pain Tracker"
VITE_APP_TITLE=Pain Tracker

# Sentry DSN for error tracking (optional)
# Sign up at https://sentry.io to get your DSN
# Leave empty to disable error reporting
# VITE_SENTRY_DSN=https://your-dsn@sentry.io

# Google Analytics Measurement ID (optional)
# Format: G-XXXXXXXXXX
# Leave empty to disable analytics
# VITE_GA_MEASUREMENT_ID=

# ================================
# Backend Secrets (NEVER use VITE_ prefix for secrets!)
# ================================

# WorkSafe BC API key for server-side requests
# This key should NEVER be exposed to the client
# Contact your system administrator or WorkSafe BC for the actual key
WCB_API_KEY=backend_only_secret

# Database connection string (if using a database)
# Example formats:
# PostgreSQL: postgresql://username:password@localhost:5432/pain_tracker
# MongoDB: mongodb://username:password@localhost:27017/pain_tracker
# SQLite: file:./pain_tracker.db
# DATABASE_URL=

# JWT secret for authentication (if implemented)
# Generate a secure random string: openssl rand -base64 32
# JWT_SECRET=

# SMTP configuration for email notifications (if implemented)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-app-password

# ================================
# Development Tools
# ================================

# Enable verbose logging in development
# Options: true, false
DEBUG=true

# Port for the development server (optional)
# Default: 3000
# PORT=3000

# Enable mock data for development/testing
# Options: true, false
# ENABLE_MOCK_DATA=false

# ================================
# Security Settings  
# ================================

# CORS origins for API requests (comma-separated)
# In production, this should be your actual domain(s)
# CORS_ORIGINS=http://localhost:3000,https://yourdomain.com

# Session secret for authentication
# Generate a secure random string: openssl rand -base64 32
# SESSION_SECRET=

# ================================
# Notes and Troubleshooting
# ================================
#
# 🔧 Common Issues:
# 
# 1. "Cannot connect to API":
#    - Check VITE_WCB_API_ENDPOINT points to correct server
#    - Ensure your API server is running
#    - Verify CORS settings if using external API
#
# 2. "Build fails with environment error":
#    - Ensure all VITE_ prefixed variables are set
#    - Check that no secrets use VITE_ prefix
#    - Run `npm run doctor` for diagnosis
#
# 3. "Authentication not working":
#    - Verify WCB_API_KEY is correct and has proper permissions
#    - Check that JWT_SECRET is set and secure
#    - Ensure SESSION_SECRET is configured
#
# 4. "Error tracking not working":
#    - Verify VITE_SENTRY_DSN format is correct
#    - Check Sentry project configuration
#    - Ensure DSN has proper permissions
#
# 📖 More help:
# - Run: npm run doctor
# - Check: README.md
# - Visit: https://github.com/CrisisCore-Systems/pain-tracker/wiki

